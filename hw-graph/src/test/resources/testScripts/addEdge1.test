# Creates a single graph and adds 2 nodes to it and an edge between the nodes

# Create a Graph
CreateGraph graph1

# Add a pair of nodes
AddNode graph1 n1
AddNode graph1 n2

# Add an edge between the two nodes
AddEdge graph1 n1 n2 e1